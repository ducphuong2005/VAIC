import{card}from'../components/layout.js';import{getProfile,getProfileCompletion,getDimensions,updateProfile}from'../api/profileApi.js';import{toProfileViewModel}from'../mappers/profileMapper.js';
let currentProfile={};
export async function profilePage(){const[p,c,d]=await Promise.all([getProfile(),getProfileCompletion(),getDimensions()]);currentProfile=p||{};const vm=toProfileViewModel(p,c,d);const hero=`<div class="profile-hero"><span class="avatar big">CC</span><div><h2>Hồ sơ nghề nghiệp</h2><p>${vm.school} · ${vm.regions.join(', ')||'Chưa cập nhật khu vực'}</p><em>${vm.education}</em><em>${vm.major}</em></div><button class="btn btn-outline" data-profile-edit>✎ Chỉnh sửa hồ sơ</button></div><div class="completion"><b>Hồ sơ đã hoàn thiện ${vm.completion}%</b><span>${vm.missing.length?`Còn ${vm.missing.length} mục cần bổ sung`:vm.status}</span><div class="progress"><i style="width:${vm.completion}%"></i></div></div>`;const info=`<div class="info-grid"><div><span>Trình độ</span><b>${vm.education}</b></div><div><span>Trường học</span><b>${vm.school}</b></div><div><span>Chuyên ngành</span><b>${vm.major}</b></div><div><span>Thời gian học mỗi tuần</span><b>${vm.weeklyHours??'Chưa cập nhật'} giờ</b></div><div><span>Khu vực mong muốn</span><b>${vm.regions.join(', ')||'Chưa cập nhật'}</b></div><div><span>Mục tiêu</span><b>${vm.goals}</b></div></div>`;const dims=vm.dimensions.length?`<div class="skills-strip">${vm.dimensions.map(x=>`<div class="skill-mini"><div><strong>${x.name}</strong><small>${x.scoreText} · Tin cậy ${x.confidence} · ${x.evidence} bằng chứng</small><div class="mini-progress"><i style="width:${x.score}%"></i></div></div></div>`).join('')}</div>`:'<div class="empty-state"><h3>Chưa có điểm năng lực</h3><p>Hoàn thành bài đánh giá hoặc mini-game để cập nhật.</p></div>';const form=`<form class="settings-section" data-profile-form hidden><h3>Chỉnh sửa hồ sơ</h3><div class="form-grid"><label>Trình độ<input name="educationLevel" value="${vm.education==='Chưa cập nhật'?'':vm.education}"></label><label>Trường học<input name="school" value="${vm.school==='Chưa cập nhật'?'':vm.school}"></label><label>Chuyên ngành<input name="currentMajor" value="${vm.major==='Chưa cập nhật'?'':vm.major}"></label><label>Số giờ học/tuần<input name="availableLearningHoursPerWeek" type="number" min="0" max="168" value="${vm.weeklyHours??''}"></label><label>Khu vực (cách nhau bởi dấu phẩy)<input name="preferredRegions" value="${vm.regions.join(', ')}"></label><label>Mục tiêu nghề nghiệp<input name="careerGoals" value="${vm.goals==='Chưa cập nhật'?'':vm.goals}"></label></div><button class="btn btn-primary" type="submit">Lưu thay đổi</button><p data-form-message></p></form>`;return`<div class="two-col"><div>${card('',hero)}${card('Thông tin cá nhân',info)}${card('Điểm mạnh nổi bật',dims)}</div><div>${card('Cập nhật hồ sơ',form)}${card('Mức độ hồ sơ',`<div class="score"><b>${vm.completion}%</b><span>${vm.status}</span></div><ul class="profile-list">${vm.missing.map(x=>`<li>○ ${x}</li>`).join('')||'<li>✓ Hồ sơ đã đầy đủ</li>'}</ul>`)}</div></div>`}
export function bindProfileEvents(){const form=document.querySelector('[data-profile-form]');document.querySelector('[data-profile-edit]')?.addEventListener('click',()=>{form.hidden=false;form.scrollIntoView({behavior:'smooth'})});form?.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button[type=submit]'),message=form.querySelector('[data-form-message]'),data=Object.fromEntries(new FormData(form));data.preferredRegions=data.preferredRegions.split(',').map(x=>x.trim()).filter(Boolean);data.availableLearningHoursPerWeek=data.availableLearningHoursPerWeek===''?null:Number(data.availableLearningHoursPerWeek);data.learningBudget=currentProfile.learningBudget;data.preferredEducationRoutes=currentProfile.preferredEducationRoutes||[];data.existingSkills=currentProfile.existingSkills||[];button.disabled=true;button.textContent='Đang lưu...';try{await updateProfile(data);message.textContent='Đã cập nhật hồ sơ.';setTimeout(()=>location.reload(),500)}catch(error){message.textContent=error.message||'Không thể cập nhật.';button.disabled=false;button.textContent='Lưu thay đổi'}})}
export{profilePage as profile};

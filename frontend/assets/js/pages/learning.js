import { card } from '../components/layout.js';
import { icon } from '../components/icons.js';

const modules=[['Nền tảng dữ liệu','Làm quen với tư duy dữ liệu, Excel và thống kê cơ bản.','4 tuần','6/8 bài',75,'📊'],['Phân tích với SQL','Truy vấn, làm sạch và tổng hợp dữ liệu từ cơ sở dữ liệu.','5 tuần','2/10 bài',20,'🗃️'],['Python cho Data Science','Xử lý và trực quan hóa dữ liệu bằng Python.','6 tuần','0/12 bài',0,'🐍'],['Machine Learning cơ bản','Xây dựng và đánh giá các mô hình dự đoán đầu tiên.','8 tuần','0/14 bài',0,'🤖']];

export function learningPage(){
 const overview=`<div class="learning-overview"><div><span class="path-badge">LỘ TRÌNH ĐỀ XUẤT</span><h2>Trở thành Data Scientist</h2><p>Lộ trình cá nhân hóa theo năng lực hiện tại của bạn · Cập nhật 15/07/2026</p><div class="path-meta"><span>⏱ 6 tháng</span><span>📚 44 bài học</span><span>🏅 4 chứng nhận</span></div></div><div class="overall-progress"><div class="big-ring small"><strong>24%</strong><span>Hoàn thành</span></div><button class="btn btn-white">Tiếp tục học ${icon('play',15)}</button></div></div>`;
 const timeline=`<div class="learning-timeline">${modules.map(([name,desc,time,lesson,progress,emoji],i)=>`<article class="module ${progress===0&&i>1?'locked':''}"><div class="module-number">${progress===100?'✓':i+1}</div><div class="module-card"><div class="module-icon">${emoji}</div><div class="module-info"><span>CHẶNG ${i+1}</span><h3>${name}</h3><p>${desc}</p><div class="module-meta"><span>⏱ ${time}</span><span>📖 ${lesson}</span></div></div><div class="module-action">${progress?`<strong>${progress}%</strong><div class="progress slim"><i style="width:${progress}%"></i></div><button class="btn btn-soft">${progress?'Tiếp tục':'Bắt đầu'} ${icon('arrow',14)}</button>`:`${icon('lock',20)}<span>Hoàn thành chặng trước</span>`}</div></div></article>`).join('')}</div>`;
 const side=`${card('Mục tiêu tuần này','<div class="weekly-goal"><strong>3/5</strong><span>bài học đã hoàn thành</span><div class="progress"><i style="width:60%"></i></div><p>🔥 Chuỗi học tập: <b>5 ngày</b></p></div>',{icon:'trophy'})}${card('Kỹ năng sẽ đạt được','<div class="learn-skills"><span>SQL</span><span>Python</span><span>Thống kê</span><span>Machine Learning</span><span>Data Visualization</span><span>Storytelling</span></div>',{icon:'spark'})}${card('Cần hỗ trợ?','<p class="muted">Hỏi AI khi bạn chưa rõ một khái niệm hoặc cần gợi ý tài liệu.</p><button class="btn btn-primary btn-block">'+icon('bot',16)+' Hỏi trợ lý AI</button>',{})}`;
 return `${overview}<div class="learning-layout"><div><div class="section-heading"><h2>Các chặng học tập</h2><span>1/4 chặng đang học</span></div>${timeline}</div><aside>${side}</aside></div>`;
}
